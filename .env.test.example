# Playwright test credentials — copy this file to .env.test and fill in.
# .env.test is gitignored. Never commit real credentials.
TEST_USER_EMAIL=test@example.com
TEST_USER_PASSWORD=your-test-password
