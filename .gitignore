node_modules
dist
dist-ssr
*.local
.env
.env.*
!.env.example

# TypeScript build artifacts
*.tsbuildinfo
vite.config.js
vite.config.d.ts

# Logs
*.log

# Vite dep pre-bundle cache
.vite

# Playwright MCP output
.playwright-mcp/
test-results/
playwright-report/

# Source snapshot — regenerated from a fresh clone before each publish
# (see "Source code" section in src/pages/LobbyPage.tsx). Tracking this
# in git would embed every previous snapshot in .git history forever.
public/wordz-source.zip

# MCP server distribution — built by `bun run build:mcp` from mcp-server/.
# Same reasoning as above: every rebuild would add another blob to history.
public/wordz-mcp.zip
